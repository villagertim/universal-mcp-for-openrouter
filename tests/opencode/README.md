# Tests — Opencode Profile

> **Status:** No tests have been created or run for the Opencode profile.

This directory is a placeholder for future tests specific to the `opencode` profile. Contributions welcome.

## What Needs Testing

- [ ] Verify that `vision_analyze` works correctly through Opencode's MCP integration
- [ ] Verify that disabled tools (`pin_context`, `index_project`, etc.) do not appear in Opencode's tool list
- [ ] Run the core test suite (`chat`, `models`, `account`, `budget`) to verify basic functionality

## How to Contribute

1. Copy relevant test files from `tests/antigravity/` as a starting point
2. Adapt for Opencode's MCP configuration
3. Run and document results
4. Submit a pull request
