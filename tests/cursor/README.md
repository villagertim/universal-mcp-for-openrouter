# Tests — Cursor Profile

> **Status:** No tests have been created or run for the Cursor profile.

This directory is a placeholder for future tests specific to the `cursor` profile. Contributions welcome.

## What Needs Testing

- [ ] Verify that disabled tools (`vision_analyze`, `index_project`, `search_symbols`, etc.) do not appear in Cursor's tool list
- [ ] Verify that `pin_context` / `retrieve_context` work correctly through Cursor's MCP integration
- [ ] Run the core test suite (`chat`, `models`, `account`, `budget`) to verify basic functionality

## How to Contribute

1. Copy relevant test files from `tests/antigravity/` as a starting point
2. Adapt for Cursor's MCP configuration
3. Run and document results
4. Submit a pull request
