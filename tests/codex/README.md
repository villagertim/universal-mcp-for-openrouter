# Tests — Codex Profile

> **Status:** No tests have been created or run for the Codex profile.

This directory is a placeholder for future tests specific to the `codex` profile. Contributions welcome.

## What Needs Testing

- [ ] Verify that disabled tools (`vision_analyze`, `index_project`, `search_symbols`, etc.) do not appear in Codex's tool list
- [ ] Verify that `pin_context` / `retrieve_context` work correctly through Codex's MCP integration
- [ ] Run the core test suite (`chat`, `models`, `account`, `budget`) to verify basic functionality

## How to Contribute

1. Copy relevant test files from `tests/antigravity/` as a starting point
2. Adapt for Codex's MCP configuration
3. Run and document results
4. Submit a pull request
